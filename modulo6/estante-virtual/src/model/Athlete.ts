export default class Athlete {
    constructor(
        private id: string,
        private name: string,
        private value: string,
        private unity: string,
        private competition_id: string
    ) { }
}